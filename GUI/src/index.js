// React
import React from 'react';
import ReactDOM from 'react-dom/client';

// Main App
import SuggestMeApp from './SuggestMeApp';

// Sass styles
import './index.scss';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <SuggestMeApp />
);
